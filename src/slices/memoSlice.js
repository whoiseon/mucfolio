import {createSlice} from "@reduxjs/toolkit";

const initialState = {};

const memoSlice = createSlice({
  name: 'memo',
  initialState,
  reducers: {},
  extraReducers: {},
});

export default memoSlice;
